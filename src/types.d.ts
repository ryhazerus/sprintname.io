interface VotingOption {
    id: number,
    name: string,
    description: string,
    image: string
}

interface Navigation {
    title: string,
    description: string | undefined,
    link: string
}